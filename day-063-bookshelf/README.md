# Bookshelf Using Flask and SQLAlchemy

## Objective
Create a simple bookshelf web app using Flask and can do CRUD operations. Database to be used is SQL using SQLAlchemy.
The result should look something like this: